import { ExpensesReportLine } from './expenses-report-line.model';

describe('ExpensesReportLine', () => {
  it('should create an instance', () => {
    expect(new ExpensesReportLine()).toBeTruthy();
  });
});
