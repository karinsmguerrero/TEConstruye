import { EmployeeSalaryLine } from './employee-salary-line.model';

describe('EmployeeSalaryLine', () => {
  it('should create an instance', () => {
    expect(new EmployeeSalaryLine()).toBeTruthy();
  });
});
