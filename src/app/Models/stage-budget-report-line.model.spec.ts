import { StageBudgetReportLine } from './stage-budget-report-line.model';

describe('StageBudgetReportLine', () => {
  it('should create an instance', () => {
    expect(new StageBudgetReportLine()).toBeTruthy();
  });
});
